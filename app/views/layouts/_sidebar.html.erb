<div id="sidebar">
  <ul>
    <% if signed_in? %>
        <li><%= link_to "Profile, current_user.name", current_user %></li>
        <%= link_to "Sign out", signout_path, method: "delete" %>
    <% else %>
        <li><%= link_to "Войти", signin_path %></li>
    <% end %>
  </ul>
</div>
